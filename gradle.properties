# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# We no longer need to auto-convert third-party libraries to use AndroidX, which slowed down the build
android.enableJetifier=false

# Allow Gradle to use up to 4 GB of RAM. Upstream ships 1 GB, which is enough for
# their single TeamCode module. This fork builds four team modules, several with
# heavy third-party libraries, and D8 runs out of heap during dexing at 1 GB.
org.gradle.jvmargs=-Xmx4096M

android.nonTransitiveRClass=false